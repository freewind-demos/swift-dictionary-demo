# Swift 字典 Demo

## 简介

展示 Swift 字典（Dictionary）的创建、操作和遍历。

## 启动和使用

```bash
cd swift-dictionary-demo
swift run
```

## 教程

### 字典特点

- 键值对存储
- 键必须可哈希（Hashable）
- 访问时返回可选值

### 常用操作

- `["key"]`: 访问/赋值
- `removeValue(forKey:)`: 删除
- `keys`/`values`: 获取所有键/值
